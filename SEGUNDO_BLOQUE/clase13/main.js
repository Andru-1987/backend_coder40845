// const lista = [1, 1, 2, 3, 4];
// lista.map((x) => x * x).forEach((x) => console.log(x));

/*

programa que genere un color aleatorio en formato (RGB) 0-255 y muestre por consola. 
vamos a llamar al programa color.js
*/
